# Screenshots

`torrin-dark.png` and `torrin-light.png` are used by the README and GitHub Pages.

Regenerate:

```bash
./scripts/prepare-and-capture-screenshots.sh
```

Fallback (no Screen Recording permission): `python3 scripts/render-readme-preview.py`
